const Input = ({type, className, style, name, placeholder, required}) => {
    return <input type={type} className={className} style={style} name={name} placeholder={placeholder} required={required} />
}

export default Input
